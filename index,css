* {
    margin: 0;
    padding: 0;

}
body {
    font-family: Arial, sans-serif;
}
/* Fix sidebar width and improve positioning */
#sidebar {
    width: 300px;
    height: 100vh;
    background: #000;
    position: fixed;
    left: 0;
    top: 0;
}

/* Clean up list styles */
ul, li {
    list-style: none;
    color: #fff;
    font-size: 20px;
    padding: 12px 24px;
}

/* Remove duplicate and conflicting rules */
ul, .nav-list {
    margin: 0;
    padding: 0;
}

/* Optional: Add hover effect for sidebar items */
#sidebar li:hover {
    background: #222;
    cursor: pointer;
}